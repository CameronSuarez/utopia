//DO NOT USE
//This file orchestrates the simulation tick (advanceTick) and is the central place where an agent's state would be evaluated and updated after its position changes. I would expect to find the "arrival detection" logic there.