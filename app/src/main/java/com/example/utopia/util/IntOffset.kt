package com.example.utopia.util

/**
 * Lightweight representation of a 2D integer coordinate.
 * Moved to its own file to avoid IDE indexing confusion with CoordUtils.
 */
data class IntOffset(val x: Int, val y: Int)
